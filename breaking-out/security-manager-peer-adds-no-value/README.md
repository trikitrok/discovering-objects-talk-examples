# Simulated Security Manager

An example where breaking out is not justified.

The collaborator `UserDataRequester` shouldn't have been treated as a peer.

It need refactoring to make it an internal.
